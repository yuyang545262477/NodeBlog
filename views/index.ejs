<%- include header.ejs %>
<% posts.forEach(function (doc, index) { %>
<h2>
    <a href="#"><%= doc.title %></a>
</h2>
<p class="info">
    作者:<%= doc.name %>
    日期:<%= doc.time.minute %>
</p>
<p><%- doc.post %></p>
<% }) %>
<%- include footer.ejs %>